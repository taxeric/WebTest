<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.kuang.mapper.ApiParamsMapper">

    <select id="selectListByUrlId" parameterType="int" resultType="com.kuang.pojo.ApiParamsEntity">
        SELECT * FROM _apiparams where parent_url_id = #{id}
    </select>
</mapper>